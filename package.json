{
  "name": "sweeble-v2",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 3000",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build && git --work-tree dist checkout --orphan gh-pages && git --work-tree dist add --all && git --work-tree dist commit -m 'deploy: update GitHub Pages build' && git push origin gh-pages --force && git checkout -f main && git branch -D gh-pages"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
